import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.galeriafotos',
  appName: 'galeria-fotos-ionic',
  webDir: 'dist'
};

export default config;
